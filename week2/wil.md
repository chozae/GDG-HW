이번 주차를 배우면서, AI를 학습시키기 위해선 데이터 전처리가 무척이나 중요하다는 것을 배울 수 있었습니다.
특히 저번 주 과제에서는 무게의 차이로 인해 잘못된 결과가 나왔습니다.
이를 해결하는 방안으로 표준 점수를 구하여, 그 거리를 정규화 해주는 방법이었습니다.
KNeighborsClassifier는 단순히 거리를 구하는 함수이기 때문에 각 데이터이 가중치가 다르면 원하는 결과가 왜곡 될 수 있다는 것을 알게 되었습니다.

과제를 하며 훈련데이터와 실습 데이터를 9:1로 나누는 실수가 있었습니다.
그러자 kn이 오류를 발생시키며 동작하지 않았습니다. 
이를 보고 머신러닝도 결국 적절한 데이터의 분리가 필수적이라는 것을 알 수 있었습니다.

그리고 numpy같은 함수를 사용하지 않고, 데이터를 처리하는 것이 얼마나 불편한지 알 수 있었습니다.

